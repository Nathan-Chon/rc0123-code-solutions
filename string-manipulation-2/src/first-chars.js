/* exported firstChars */
/*
Define a function, named 'firstChars', that accepts 2 arguements, 'length', 'string'.
    Return the value of string that is sliced at the length of the result.
*/
function firstChars(length, string) {
  return string.slice(0, length);
}
