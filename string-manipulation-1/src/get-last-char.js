/* exported getLastChar */
/*
Define a function, named 'getLastChar', that uses one argument, named 'string'.
  return the last letter of the string.
*/
function getLastChar(string) {
  return string[string.length - 1];
}
