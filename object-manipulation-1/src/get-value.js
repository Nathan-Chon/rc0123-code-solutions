/* exported getValue */
/*
Define a function named 'getValue', that accepts one arguement, 'object', 'key'.
  Return the value of 'key' variable located in the 'object' variable.
*/
function getValue(object, key) {
  return object[key];
}
